package sold.monkeytech.com.sold_android.services;

public class ConnectivityChangeReceiver {//extends BroadcastReceiver {

//    @Override
//    public void onReceive(Context context, Intent intent) {
//        Log.d("wowConnection", "onReceive");
//        Intent i = new Intent(context, YourService.class);
//        i.putExtra("isNetworkConnected", isConnected(context));
//        context.startService(i);
//    }
//
//    public boolean isConnected(final Context context) {
//        ConnectivityManager connectivityManager = ((ConnectivityManager)context.getSystemService(Context.CONNECTIVITY_SERVICE));
//        NetworkInfo networkInfo = connectivityManager.getActiveNetworkInfo();
//        boolean isConnected = networkInfo != null && networkInfo.isAvailable() && networkInfo.isConnected();
//        Log.d("wowConnection", "isConnected: " + isConnected);
//        return isConnected;
//   }

}